# code_DL_merge_test_V1.3

## 用途

本目录是深度学习算法的现场测试工程，用于验证 C800、129 ROI、三线程正式照片处理和 DS 结算，并输出算法诊断及耗时日志。

V1.3在V1.2完整流程上新增RKNN/NPU推理后端，OpenCV继续使用3.4.12。

## 基线

- 基于线上 `develop` 提交 `eed65070544fcd7e0475fe9248d6b43b15d808aa`。
- 该提交备注为“正式照片处理优化为三线程并行”。

## 与基线的差异

1. `algo/algo.cpp`：C800 未检出任一目标眼时，直接输出 `PupilNotFound`，复用原有停灯和返回预览流程。
2. 同步 V1.1 的纯日志低频化代码：关闭默认逐帧抓图和预览日志，正常调度详情由详细开关控制，正式失败详情每轮最多输出两条，并保留轮次及整次测量耗时汇总。
3. `measure/exposure-adjuster.cpp/.h`：同步 V1.1 的首次快速候选、比例粗调、固定步长精调和曝光耗时统计。
4. `winmeasure.cpp`：同步曝光参数下发及曝光流程结束的计时调用，不引入 V1.1 的测量恢复状态机。
5. 增加 `mainbasic_DL_merge_test_rk3568.pro`，内容与基线 `mainbasic_rk3568.pro` 一致，便于在 Qt 中区分测试工程。
6. 新增 `algo/pupil_pair_rknn_detector.cpp/.h`：使用 RKNN Runtime 2.3.2 在 RK3568 NPU 上执行 C800。
7. C800统一改为NPU优先；NPU模型缺失、初始化失败或运行期推理失败时，自动回退现有ONNX CPU后端。
8. ONNX与RKNN共用同一套400×160加黑图预处理、连通域后处理和坐标映射，正式拍照、129 ROI、Small Match及DS规则不变。

## V1.3运行依赖

- RKNN模型：`/root/screener-models/pupil/releases/spatial_concat_800x160_dataset12/c800_fp16.rknn`
- ONNX回退模型：`/root/screener-models/pupil/releases/spatial_concat_800x160_dataset12/model_fp32.onnx`
- RKNN运行库：`/root/screener-libs/librknnrt.so`
- NPU驱动：当前验证版本为`0.8.8`
- RKNN Runtime：`2.3.2`

## 未修改内容

- 129 ROI 失败规则保持 develop 原逻辑。
- 三线程正式照片处理、DS、存图、测距和串口流程保持 develop 原逻辑。
- 未同步 V1.1 的测量令牌、恢复轮、延迟重启和迟到回调状态机。

## Qt 工程入口

打开：`srcs/mainbasic/mainbasic_DL_merge_test_rk3568.pro`

## 曝光冷启动测试

- `measure/exposure-adjuster.h` 中的 `ENABLE_EXPOSURE_COLD_START_TEST` 默认设为 `1`。
- 开启后，每次进入预览都会先恢复统一默认曝光，再执行现有快速候选、比例粗调和固定步长精调，不复用上一场测量的可靠曝光。
- 现场测试完成、需要恢复历史曝光复用时，将该宏改为 `0` 即可，无需修改工程文件。
- 最终 `[ExposureTiming]` 汇总会输出 `cold_start_test`、`cold_start_applied`、`cold_start_exposure_us`，以及从预览首次识别到瞳孔至曝光稳定的 `pupil_detected_to_stable_ms`。

## 当前已知问题

1. C800 未识别出目标瞳孔后能够返回预览，但失败回调较晚时，控制层可能已经启动下一物理轮，造成一次无效转灯，并重复输出 `measurement already completed`。
2. 正常结果回调到达前，控制层可能提前启动第三个物理轮，增加可见转灯次数。
3. 部分轮次会因 Small Match 或 129 ROI 大量失败而被 DS 以 `too_many_missing` 拒绝，使首张到结果耗时由正常约 1.56 秒增加到约 3.0～3.3 秒。
4. 曝光调整存在长尾：首次有效调整约 2.60 秒，部分场景达到 7.61 秒；日志中出现过一次持续 52.06 秒、调整 57 次后被中断的情况。
5. 本次日志未发现 SIGSEGV、断言失败、线程归属错误或算法结果回调超时。
